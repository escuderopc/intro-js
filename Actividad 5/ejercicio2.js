//Declarar un arreglo vacio con un ciclo while que se ejecute 5 veces, agregar los elementos al array
var array = []
var cont = 0
while (cont<=4) {
 array += prompt("Agrega un elemento al array ")
 cont++
}console.log("Los elementos del array son: "+array)